<article itemscope itemtype="http://data-vocabulary.org/Event">
  <%= link_to image_tag("icons/back.png"), :back, :id =>"backIcon" %>
  <p itemprop="summary">
    <b><%= t :name %></b>
    <%= @event.name %>
  </p>

  <p>
    <b><%= t :start_date %></b>
    <time itemprop="startDate" datetime= <%= @event.startdatetime.to_formatted_s(:long) %> > <%= @event.startdatetime.to_formatted_s(:long) %> </time> 
  </p>
  <p>
    <b><%= t :duration %></b>
    <time itemprop="duration" > <%= distance_of_time_in_words(@event.duration) %> </time> 
  </p>
  <p itemprop="location" itemscope itemtype="http://data-vocabulary.org/Organization">
    <% if @event.location.present? %>
    <b><%= t :location %></b>
    <span itemprop="name">  <%= link_to @event.location.name, location_path(@event.location.id) %> </span>
    <% end %>
  </p>
  <p itemprop="name" itemscope itemtype="http://data-vocabulary.org/Organization">
    <% if @event.organizer.present? %>
    <b><%= t :organizer %></b>
    <%= link_to @event.organizer.name, organizer_path(@event.organizer.id) %>
    <% end %>
  </p>

  <p class="text-wrap" itemprop="description">
    <b><%= t :description %></b>
    <%= @event.description %>
  </p>

  <p itemprop="eventType">
    <b>Event type:</b>
    <%= @event.event_type %>
  </p>

  <p itemprop="employee" itemscope itemtype="http://schema.org/Organization">
    <b><%= t :responsible %></b>
    <%= @event.responsible %>
  </p>

  <p><b>Public</b>
    <%= boolean_to_words(@event.public) %>
  </p>

  <p>
    <b><%= t :tags %></b>
    <% @event.tags.each do |tag| %>
    <%= tag.name %>
    <% end %>
  </p>

  <p>
 <%= link_to image_tag('icons/edit.png'), edit_event_path(@event) %>
  </p>
</article>
